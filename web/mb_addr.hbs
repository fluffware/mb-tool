{{#if addr includeZero=true}}
  {{addr}}
  {{#if rel includeZero=true}}
    ({{rel}})
  {{/if}}
{{else}}
  {{addr_low}} - {{addr_high}}
  {{#if rel_low includeZero=true}}
    ({{rel_low}} - {{rel_high}})
  {{/if}}

{{/if}}
